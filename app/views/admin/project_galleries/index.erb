<%= link_to 'New Project Gallery', new_admin_project_gallery_path, class: 'btn btn-block btn-primary' %>
<div class="divided-20"></div>
<table data-toggle="table" data-classes="table table-hover table-condensed"
       data-striped="true">
  <thead>
  <tr>
    <th data-field="id" data-sortable="true">Name</th>
    <th data-field="name" data-sortable="false">Photos</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @project_galleries.each do |gallery| %>
      <tr>
        <td><%= gallery.name %></td>
        <td><%= gallery.project_photos.count %></td>
        <td>
          <%= link_to 'Edit', edit_admin_project_gallery_path(gallery), class: 'btn btn-primary btn-sm' %>
          <%= link_to 'Delete', admin_project_gallery_path(gallery), class: 'btn btn-danger btn-sm', method: :delete, data: {confirm: 'Are you sure?'} %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
