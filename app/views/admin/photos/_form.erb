<%= simple_form_for([:admin ,@photo])  do |f| %>
    <%= f.association :gallery, label: 'Gallery' %>
    <%= f.input :photo, label: 'Photo', as: :file %>
    <%= f.button :submit, @photo.new_record? ? 'Add Photo' : 'Update Photo', class: 'btn btn-success btn-block btn-medium' %>
<% end %>